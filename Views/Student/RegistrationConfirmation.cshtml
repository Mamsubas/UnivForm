@{
    ViewData["Title"] = "Kayıt Tamamlandı";
    var confirmationLink = TempData["ConfirmationLink"] as string;
}

<div class="max-w-md mx-auto mt-12 bg-white rounded-lg shadow-md p-8 text-center">
    <div class="mb-6">
        <svg class="w-16 h-16 mx-auto text-blue-500" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
        </svg>
    </div>
    <h1 class="text-2xl font-bold text-gray-900">Kayıt Tamamlandı!</h1>
    <p class="text-gray-600 mt-4">
        Lütfen kontrol edin. E-posta adresinize bir doğrulama bağlantısı gönderdik.
    </p>
    <p class="text-gray-500 text-sm mt-2">
        E-posta adresinizi doğrulamak için bağlantıya tıklayın, ardından giriş yapabilirsiniz.
    </p>

    @if (!string.IsNullOrEmpty(confirmationLink))
    {
        <div class="mt-6 bg-amber-50 border border-amber-200 rounded-lg p-4 text-left">
            <p class="text-sm font-semibold text-amber-900 mb-2">📧 Geliştirme Modu - Doğrulama Bağlantısı:</p>
            <a href="@confirmationLink" class="text-blue-600 hover:underline break-all text-xs">
                @confirmationLink
            </a>
            <p class="text-xs text-amber-700 mt-2">Lütfen linke tıklayın veya tarayıcınıza kopyalayıp yapıştırın.</p>
        </div>
    }

    <div class="mt-6 space-y-3">
        <a href="/" class="block w-full bg-indigo-600 text-white py-2 rounded-lg hover:bg-indigo-700">
            Anasayfaya Dön
        </a>
    </div>
</div>
